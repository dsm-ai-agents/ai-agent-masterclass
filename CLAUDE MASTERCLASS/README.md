# Prompt 1
```
What do you see in my folder, provide brief summary
```

# Prompt 2
```
So I want you to create, um, a Claude dot MD file for this project that we are doing. In this project, we are going to take the raw data and create PowerPoint presentation and a HTML dashboard that managers can use. And the PowerPoint is going to be one slider executive summary for executive presentation. And, uh, the HTML is going to be, um, you know, dashboard, uh, that managers can consume, uh, depending on the region they are on. Okay? So agent one will essentially take the raw data, the input data, and convert it into a combined table. Agent two will then take the combined table and create a summary out of it, uh, that will go into the PowerPoint presentation, and it will go into the HTML report. Agent three will create the PowerPoint presentation. Agent four will create the HTML report.
```

# Prompt 3
```
Okay. So now I want you to create the agent one dot underscore, you know, merge agent two underscore, you know, summary agent two underscore PPT... agent three underscore PPT, agent four under score HTML, MD files so that we can replicate this work.
```

# Prompt 4
```
activate 3 and 4 - use 2 sub agents to do the work in parallel
```